spring:
  datasource:
    url: jdbc:postgresql://localhost/literalura
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: false